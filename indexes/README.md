# Indexes

This folder is for generated local indexes and process state.

Private/generated files in this folder are ignored by Git. Public documentation files such as this README may be committed.

Current generated file:

```text
inbox-process-state.json
```

That file tracks which inbox entries have already been processed by each processor and model. It may contain private entry IDs and should stay local.

